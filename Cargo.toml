[package]
name = "mst-bench"
version = "0.1.0"
edition = "2024"

[dependencies]
rayon = "1.10"
clap = { version = "4", features = ["derive"] }
csv = "1.3"
petgraph = "0.7"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
